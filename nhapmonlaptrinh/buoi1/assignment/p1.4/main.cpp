#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    double x;
    cin >> x;
    cout << fixed << showpoint;
    cout << setprecision(2);
    cout << "Chu vi: "<<x*4 << endl <<"Dien tich: " << x*x;
    return 0;
}
